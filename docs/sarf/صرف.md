---
title: صرف
encrypt: true
---
Barcode scan buttons are missing from BOTH integration points that were supposed to be added — src/components/ProductPicker.tsx (invoice line entry, used by sale/purchase invoice screens) and src/app/products/new.tsx / \[id].tsx (product form barcode field). This was previously reported as fully implemented, but isn't showing up in the running app for either location.



STEP 1 — VERIFY WHAT ACTUALLY EXISTS

Check and report back honestly, file by file:

1. Does src/components/BarcodeScannerModal.tsx exist and look complete (CameraView, permission handling, onScanned callback)?

2. Does getProductByBarcode() exist in src/db/repositories/products.ts?

3. Does src/components/ProductPicker.tsx actually import BarcodeScannerModal and render a scan button in its JSX? Paste the current full contents of ProductPicker.tsx.

4. Does src/app/products/new.tsx actually import BarcodeScannerModal and render a scan button near the barcode field? Paste the current full contents of that file.

5. Does src/app/products/\[id].tsx have the same? Paste its current full contents too.



Do not summarize — paste the actual file contents for items 3-5, since a prior summary claimed this was done and it demonstrably isn't visible in the running app, so the summary can't be trusted without the real code this time.



STEP 2 — DIAGNOSE

Based on what STEP 1 reveals, this is one of:

(a) The button code was never actually written to these files despite being reported as done, OR

(b) The button code exists but has a rendering bug (wrong import path causing a silent failure, conditional that's never true, zero-size/invisible styling, wrapped in a component that clips it), OR

(c) The code exists and is correct, but the running app is on a stale Metro bundle that never picked up the change (less likely across three separate files, but check by doing a full reload, not fast refresh, before concluding it's a code problem).



STEP 3 — FIX

Whichever cause it is, fix it so all three integration points actually render and function:

\- ProductPicker.tsx: 📷 button next to the search input, opens BarcodeScannerModal, looks up getProductByBarcode() on scan, calls onAdd() if found or shows the "product not found, add new?" Alert if not, exactly per the original spec.

\- products/new.tsx and \[id].tsx: 📷 button next to the "الباركود" FormField, opens BarcodeScannerModal, fills the field on scan, checks for duplicate barcodes before save.



STEP 4 — PROVE IT

After fixing, paste the actual updated JSX for the relevant button+field/input block in all three files (not the whole file, just the relevant ~10-15 lines each), and confirm testing was done with a full app reload in Expo Go, not just a saved file / fast refresh.
